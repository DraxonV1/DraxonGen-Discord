# Changelog

## v7.0 
- Introduced the unified MuskServices DTA promotion with worker earnings at $0.02 per EV, detailing how to join the worker lane, earn via simple generation tasks, and invite sub-workers for extra affiliate income.
- Documented the private lane pricing and bulk discounts (`2k+` credits at $0.018, `5k+` at $0.015) so private members can top up their standalone balance and generate unlimited EVs with per-run credits.
- Streamlined the worker vs. private command tree, export options, and `/help` surface so users see only the commands they can access and admins can drill into every command.
- Added GitHub release automation and promotional materials, including a new workflow that packages `tool.exe` and the changelog for tagged releases.
